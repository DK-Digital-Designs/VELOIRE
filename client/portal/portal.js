/**
 * VELOIRE — Client Portal Logic
 */
import { api } from '../js/api.js';
import { ui } from '../js/ui.js';

document.addEventListener("DOMContentLoaded", () => {
    console.log("Portal initialized");
    // In a real app, we'd fetch member-specific data here.
});
